import type { StylesGroup } from "@/lib/theme";

export const styles: StylesGroup = {
  root: {
    width: "100%",
    height: "100%",
    gap: 4,
    alignItems: "center",
  },
  removeButton: {
    color: "text.primary",
  },
};
