import { StylesGroup } from "@/lib/theme";

export const styles: StylesGroup = {
  root: {
    zIndex: 1000,
    position: "fixed",
    left: "6rem",
    bottom: "0.25rem",
    alignItems: "flex-end",
  },
};
